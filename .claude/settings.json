{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash(git commit*)",
        "hooks": [
          {
            "type": "command",
            "command": "cargo fmt --all -- --check && scripts/check-pii.sh && scripts/check_readme_mirror.sh && cargo test --workspace --quiet 2>&1 && cargo clippy --all-targets --quiet -- -D warnings 2>&1"
          }
        ]
      }
    ]
  }
}
