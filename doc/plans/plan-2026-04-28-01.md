# Plan 23 — std-time Duration family inside `time/`

## Goal

Land four Galois connections bridging `std::time::Duration` to numeric
types, hosted in the existing `src/time/duration.rs`, and reframe the
`time/` submodule's identity from "the `time` crate's Conns" to
"time-domain Conns spanning the `time` crate and `std::time`".

## Dependency Graph

```
T1 (STDRU064/U128 seconds+nanos) ─┐
                                  ├─► T3 (re-exports + module docs) ─┐
T2 (F064STDR/F032STDR float secs) ┘                                  │
                                                                     │
T4 (proptest property wiring) ───────────────────────────────────────┤
                                                                     │
T5 (CLAUDE.md §Module organization update) ──────────────────────────┤
                                                                     │
T6 (README mention) ─────────────────────────────────────────────────┴── /sprint-review → push → MR
```

## Tasks

### T1 — `STDRU064` and `STDRU128` Conns (whole seconds + nanoseconds)

Add to `src/time/duration.rs`. Disambiguate the two `Duration` types
via `use std::time::Duration as StdDuration;` near the top.

- **`STDRU064`**: `Conn<StdDuration, Extended<u64>>`.
  - `floor(d) = Finite(d.as_secs())` for finite `d` with no subsec rest;
    `Finite(d.as_secs())` when `subsec_nanos == 0`; else floor of
    integer seconds (which is `as_secs()` for unsigned).
  - `ceil(d) = Finite(d.as_secs() + (subsec_nanos > 0) as u64)`,
    saturating to `PosInf` if `as_secs() == u64::MAX && subsec_nanos > 0`.
  - `inner(Finite(s)) = StdDuration::from_secs(s)`.
  - `inner(NegInf) = StdDuration::ZERO` (saturating; no negative
    representatives), `inner(PosInf) = StdDuration::MAX`.
- **`STDRU128`**: `Conn<StdDuration, Extended<u128>>`.
  - On Finite: `floor = ceil = d.as_nanos()` — bijection on the
    representable range. Degenerate-Galois (the bijection is its own
    Galois adjoint).
  - `inner(Finite(n))`: split `n` into `(secs = n / 10^9, subsec_nanos
    = n % 10^9)` and call `StdDuration::new(secs as u64, subsec_nanos
    as u32)`. Saturate to `StdDuration::MAX` if `secs > u64::MAX`.
  - `inner(NegInf) = ZERO`, `inner(PosInf) = MAX`.

Both Conns saturate cleanly because `StdDuration` has no signed
representation. The `Extended<u64>` rung mirrors `DURNSECS`'s
`Extended<i64>` shape so the contrast (signed vs unsigned overflow
arms) is visible in adjacent code.

### T2 — `F064STDR` and `F032STDR` (float seconds ↔ Duration)

Add to `src/time/duration.rs`, immediately after T1's constants.

- Extend the existing `def_walk_helpers!` invocations: today they
  drive walks against `time::Duration` via `shift_duration` and
  `duration_to_f64` / `duration_to_f32`. Add std-Duration analogues:
  - `shift_std_duration(n: i32, d: StdDuration) -> StdDuration` —
    nanosecond shift saturating at `StdDuration::ZERO` and
    `StdDuration::MAX` (no negative arm).
  - `std_duration_to_f64(d: StdDuration) -> f64`,
    `std_duration_to_f32(d: StdDuration) -> f32` — use
    `d.as_secs_f64()` / `as_secs_f32()` (these are total).
- Two new `def_walk_helpers!` invocations: `f64_stdr_walks`,
  `f32_stdr_walks`.
- Two new `pub const`: `F064STDR: Conn<F064, Extended<StdDuration>>`,
  `F032STDR: Conn<F032, Extended<StdDuration>>`.
- Boundary handling: `inner(Bot)` of the float Conn produces
  `Extended<StdDuration>::NegInf`; `inner(NegInf)` of the rung side
  saturates to `StdDuration::ZERO`. Negative input floats (e.g.
  `-1.0_f64`) project to `NegInf` because the unsigned StdDuration
  has no representative below ZERO. This asymmetry vs `F064DURN`
  (which lands in signed `time::Duration`) needs an explicit doctest
  showing what `F064STDR.ceil(F064::from(-0.5))` yields.

### T3 — `time/` module re-exports + docstring reframe

`src/time/duration.rs` module docstring: extend the current
"signed-Duration connections" framing to cover both signed
`time::Duration` and unsigned `std::time::Duration`. Add a
sub-table contrasting the two rung shapes (`Extended<i64>` for
signed-saturate-both-ends vs `Extended<u64>` for unsigned-saturate-
top-only).

`src/time.rs`:
- Re-export the four new constants alongside the existing seven:
  `pub use duration::{DURNSECS, F032DURN, F064DURN, STDRU064,
  STDRU128, F064STDR, F032STDR};`.
- Reframe the module docstring opener from "Galois connections among
  the [`time`](https://docs.rs/time) crate's calendar / clock /
  duration types" to "Galois connections among time-domain types,
  spanning both the [`time`](https://docs.rs/time) crate and
  [`std::time`](https://doc.rust-lang.org/std/time/)".
- Add `STDR` to the §Naming convention prefix table (4L+0D shape,
  meaning "std::time::Duration").
- Append four rows to the §Constants table covering `STDRU064`,
  `STDRU128`, `F064STDR`, `F032STDR`.
- The README example block at the bottom of the docstring keeps
  using `DURNSECS` (the canonical entry point); a second sub-block
  shows `STDRU064` for the unsigned counterpoint.

### T4 — Proptest wiring

The four new constants need:
- `arb_std_duration()` strategy in `src/prop/arb.rs` (or
  `src/time/duration.rs`'s `#[cfg(test)] mod tests` block,
  depending on existing local-vs-shared placement). Bias toward
  `ZERO`, `MAX`, sub-second values, and exact-second values.
- `proptest!` blocks in `time/duration.rs::tests` running the full
  `prop::conn` law battery for each Conn. Mirrors the existing
  `f64_durn_proptests` / `f32_durn_proptests` shape.
- The float-Duration Conns reuse `def_walk_helpers!`'s plateau-
  property test pattern.

### T5 — CLAUDE.md §Module organization

In the table:

```
| `time`    | `time`       | `clock.rs`, …, `offset.rs` |
```

becomes:

```
| `time`    | `time`, `std::time` | `clock.rs`, `date.rs`, `datetime.rs`, `duration.rs`, `offset.rs`, `system_time.rs` (deferred) |
```

Add a paragraph after the table noting the principle: **modules are
keyed by host crate by default, except where a single domain is
meaningfully shared across crates**, citing `time/` as the worked
example. Forward-references Plan 25 (fixed merge) which applies
the same principle to `int` ⊕ `fixed`. `system_time.rs` is listed in
the file column with `(deferred)` because Plan 23 doesn't ship it —
see Deferred section.

### T6 — README mention

Append one bullet to the README's "What's shipped" section under the
time entry: "and the std-time Duration family (`STDRU064`,
`STDRU128`, `F064STDR`, `F032STDR`) for users on `std::time`."

## Verification

### Properties (must pass)

| Property | Module | Invariant |
|----------|--------|-----------|
| `stdru064_galois` | `time::duration::tests` | full `prop::conn` law battery on `STDRU064` |
| `stdru128_galois` | `time::duration::tests` | full battery on `STDRU128` |
| `stdru128_round_trip` | `time::duration::tests` | `STDRU128.ceil(STDRU128.inner(Finite(n))) == Finite(n)` for valid `n` (bijection on Finite) |
| `f64_stdr_galois` | `time::duration::tests` | full battery on `F064STDR` |
| `f32_stdr_galois` | `time::duration::tests` | full battery on `F032STDR` |
| `f64_stdr_neg_input` | `time::duration::tests` | for any `f` with `f < 0`, `F064STDR.ceil(F064::from(f)) == NegInf` (negative floats can't land on unsigned Duration) |
| `f64_stdr_plateau` | `time::duration::tests` | `def_walk_helpers!` plateau invariant: ceil-then-floor stays inside the float-precision plateau |

### Spot checks

Spot checks are stated against the **post-deviation** types: STDRU064
and STDRU128 wrap their source as `Extended<StdDuration>`, and the
F???STDR negative-input arms differ for `ceil` vs `floor`.

- `STDRU064.ceil(Finite(StdDuration::from_secs(5) + StdDuration::from_nanos(1))) == Finite(6)`
- `STDRU064.floor(Finite(StdDuration::from_secs(5) + StdDuration::from_nanos(1))) == Finite(5)`
- `STDRU064.ceil(Finite(StdDuration::MAX)) == PosInf` (subsec > 0 at MAX, so ceil pushes past `u64::MAX`)
- `STDRU064.inner(NegInf) == NegInf` (source-side wrap means the synthetic NegInf round-trips, not collapses to `StdDuration::ZERO`)
- `STDRU128.floor(Finite(StdDuration::MAX)) == Finite(StdDuration::MAX.as_nanos())` (Finite, exact, no overflow)
- `F064STDR.ceil(F064::from(-0.5_f64)) == Finite(StdDuration::ZERO)` (Galois-forced: unsigned rung has no representative below ZERO)
- `F064STDR.floor(F064::from(-0.5_f64)) == NegInf`
- `F064STDR.inner(Finite(StdDuration::from_secs(1))) == F064::from(1.0)` (exact, no ULP slack: 1 second is exactly representable in f64)

### Build gates

- `cargo build` — clean
- `cargo test --workspace` — all green, no `#[ignore]`
- `cargo clippy --all-targets -- -D warnings` — clean
- `cargo doc --no-deps` — no rustdoc warnings; new constants render
  with their doctests
- `cargo test --doc` — new doctests in `time/duration.rs` and the
  README block run green

End-to-end: a downstream user imports
`connections::time::{STDRU064, STDRU128}` and bracket-converts a
`std::time::Duration` of 1.5s through both, observing `Finite(1)` /
`Finite(2)` from `STDRU064` and `Finite(1_500_000_000)` from
`STDRU128`.

## Deferred

- **`STIMI128` (SystemTime ↔ unix nanos)** — needs platform-bounds
  decision (portable subset vs platform-actual). New file
  `time/system_time.rs` is reserved by name in the CLAUDE.md table
  but not created in this sprint. A separate follow-up sprint
  picks it up once the platform policy is settled.
- **`DURNSTDR` cross-crate Conn** (`Extended<time::Duration>` ↔
  `std::time::Duration`, signed → unsigned with negatives saturating
  to zero). Bonus from the survey; ship as a follow-up sprint once
  the merged-module pattern is settled.
- **README quick-tour example for std-time** — not needed for 0.1.x
  patch series. Pick up in the next docs sweep.
- **`system_time.rs` reservation row in CLAUDE.md** — the original plan
  asked for a forward-reference `system_time.rs (deferred)` entry. Held
  off so the table reflects what actually exists today; will land with
  the `STIMI128` follow-up.

## Review

### Design deviations from the plan

1. **Source-side `Extended` wrap on STDRU064 / STDRU128.** The plan
   specified `Conn<StdDuration, Extended<u64>>` and
   `Conn<StdDuration, Extended<u128>>`. As written those aren't
   Galois-lawful: with `inner(NegInf) = StdDuration::ZERO` (the only
   StdDuration ≤ everything), Galois forces `ceil(StdDuration::ZERO) =
   NegInf` (the same trick `DURNSECS` uses at `Duration::MIN`). At
   `MIN` that's a deeply-saturated edge; at `ZERO` it's the most-common
   input and would be a real footgun. Wrapped both sides instead —
   `Conn<Extended<StdDuration>, Extended<u64>>` and
   `Conn<Extended<StdDuration>, Extended<u128>>` — matching the
   `OFDTNANO`/`OFDTSECS` shape. `ceil(Finite(ZERO))` is now naturally
   `Finite(0)`. The constant names are unchanged because the `STDR`
   half is the all-letter `ABCD` shape that elides the `Extended` wrap
   (same convention `OFDT` uses).
2. **F???STDR negative-input semantics.** The plan said negative
   floats project to `NegInf` for both `ceil` and `floor`. With
   `inner(NegInf rung) = ExtendedFloat::Bot` (the synthetic source
   bottom), Galois forces `ceil(Extend(v < 0)) = Finite(StdDuration
   ::ZERO)` and `floor(Extend(v < 0)) = NegInf` — asymmetric. The
   doctests, spot tests, and a dedicated proptest
   (`f64_stdr_negative_input_ceil_to_zero`) assert the corrected arms.
3. **STDRU128 law battery uses an in-range strategy.** The rung
   `Extended<u128>` extends past `StdDuration::MAX.as_nanos()`
   (≈ 1.84×10²⁸ vs `u128::MAX` ≈ 3.4×10³⁸). On the bijection's image
   `STDRU128` is lawful, but `inner` saturates outside that image and
   the Galois law breaks at `(Finite(MAX), Finite(MAX.as_nanos() + 1))`
   (proptest hit this on the first run). Switched the
   `STDRU128.{galois_l/r,kernel_l/r,monotone_r}` strategy from
   `arb_extended_u128` to `arb_extended_stdr_nanos_in_range` (the same
   pattern `OFDTNANO` uses with `arb_unix_nanos_in_range`). The full
   `arb_extended_u128` strategy is kept in `prop::arb` for downstream
   use.

### Ignored properties

None — every proptest in the new battery is enabled.

### Recommendations

- A future `DURNSTDR` cross-crate Conn (deferred above) will need the
  same source-wrap analysis. The `OFDTNANO` saturation pattern is the
  template; reach for it when adding any unsigned-rung Conn whose
  source has only one open end.
- The `arb_extended_u64` / `arb_extended_u128` strategies added here
  belong to a small unsigned-Extended family that the rest of `prop/`
  should grow into uniformly when a downstream Conn wants them.
