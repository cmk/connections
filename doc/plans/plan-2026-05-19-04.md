# Plan 04 — README badge cleanup, 2-kinded prose, alias uniformity

## Goal

Land three small docs / naming-hygiene drift fixes on `main`: replace the
404-rendering crates.io / docs.rs badges with non-broken placeholders;
break the dense first paragraph of `## 2-kinded connections` into
skim-able chunks plus a const/struct convention callout; remove the
duplicate `pub type F016` and document the `fixed::iN`/`fixed::uN`
per-host `I###`/`U###` alias layer as an explicit exception to the
crate-level Conn-name convention.

## Dependency Graph

```
T1 (badges)   ─┐
T2 (prose)    ─┤
T3 (F016 dedup) ─┼─► T5 (gates) ─► finalize-docs ─► local-review
T4 (fixed alias docs) ─┘
```

T1–T4 are independent file-level changes that share the same gate run
(T5). No proptest commitments are altered.

## Tasks

### T1 — Badges

**File**: `README.md` (lines 1–2)

Swap dynamic shields for static placeholders that share the same visual
slot. The crates.io link target stays so it auto-resolves on publish.
The docs badge repoints at GitHub Pages (already shipped by
`.github/workflows/pages.yml`).

```markdown
[![crates.io](https://img.shields.io/badge/crates.io-unreleased-lightgrey.svg)](https://crates.io/crates/connections)
[![docs](https://img.shields.io/badge/docs-github.io-blue.svg)](https://cmk.github.io/connections/)
```

Plus an HTML comment immediately above the badge block:

```markdown
<!-- TODO: on first crates.io publish, swap badges back to dynamic
     shields.io/crates/v/connections.svg + shields.io/docsrs/connections -->
```

MSRV + CI badges, and the prose `Read the docs [here](...)` line at
line 6, stay unchanged.

### T2 — Reformat the `## 2-kinded connections` opener

**File**: `README.md` (lines 136–153, plus a one-line forward-pointer
in the `## L & R kind connections` section around line 113).

Break the existing single block into three short paragraphs, a tight
bulleted list of the three traits, and a `> Convention.` callout. The
SMT-verification claim, `Sandwich inequality` link, and trait
spellings are preserved verbatim. The const/struct convention is
stated explicitly inside 2-kinded and forward-pointed (once) from the
earlier kind section — user-budgeted at twice total.

### T3 — Remove duplicate `pub type F016`

**Files**: `src/core/f016.rs`, possibly `src/lib.rs`

`src/float.rs` already exposes `pub type F016 = ExtendedFloat<f16>;`
under `#[cfg(feature = "f16")]`; `src/core/f016.rs` re-declares the
same alias. Delete the duplicate; replace any in-file uses with
`crate::float::F016` (or `use crate::float::F016;`). Keep `lib.rs`'s
Naming table prose-only for `F016` to preserve the default-feature
doc build that the prior sprint set up.

### T4 — Document the `fixed::iN`/`fixed::uN` per-host alias layer

**Files**: `src/fixed.rs` (module-level docs), `src/lib.rs` (cross-link)

Add a `### Per-host type aliases` paragraph to `src/fixed.rs`'s
`//!` doc comment, clarifying that the `I###`/`U###` aliases in
`fixed::iN`/`fixed::uN` modules are a per-host wrapper layer
(letter = host signedness, digits = frac level), distinct from the
crate-level Conn-name convention (where `I###`/`U###` are reserved
for std-primitive sides and `Q###` for Q-format wrapper sides). The
two namespaces never collide because the per-host aliases are reached
through qualified module paths and Conn-name sides only appear inside
8-char Conn const identifiers.

Cross-link from `src/lib.rs`'s Naming section with one sentence.

No code changes — audit confirmed all 10 `fixed::*` files already
follow the documented pattern uniformly.

### T5 — Gates

After T1–T4:

- `cargo build --features fixed,time,hifi,macros`
- `cargo build --features fixed,time,hifi,macros,f16`
- `cargo test --features fixed,time,hifi,macros`
- `cargo clippy --all-targets --features fixed,time,hifi,macros -- -D warnings`
- `RUSTDOCFLAGS=-D warnings cargo doc --no-deps` (default features) —
  **must pass without f16**; this is the gate that caught the F016-link
  regression in the prior sprint.
- `RUSTDOCFLAGS=-D warnings cargo doc --no-deps --features f16,fixed,time,hifi,macros`
- `scripts/check_readme_mirror.sh`
- `scripts/check_pii.sh`

## Verification

### Properties (must pass)

| Property        | Module    | Invariant                       |
|-----------------|-----------|---------------------------------|
| (existing 4950) | full tree | all sprint-2 properties still green |

No new proptest properties shipping — docs + dedup only.

### Spot checks

- `target/doc/connections/index.html` Naming table — `F016` prose,
  `F032`/`F064` linked, no broken intra-doc refs.
- `target/doc/connections/fixed/index.html` — Per-host alias paragraph
  visible above the submodule index.
- README rendered via GitHub UI / local md preview — badge row shows
  two non-404 images, `## 2-kinded connections` chunks read cleanly.

### Build gates

See T5 above — all must be clean before push.

## Deferred

- **Re-export policy at crate root.** Adding
  `pub use float::{F016, F032, F064}` at crate root and/or
  `pub use {iNNN::*, ..., uNNN::*}` in `src/fixed.rs` to surface the
  aliases without deep qualified paths. Current "deep-paths-only"
  policy is uniform, so no fix is strictly required; ergonomic
  improvement is a separate sprint when prelude policy is revisited.
- **Switching badges back to dynamic shields post-publish.** Left as
  an HTML-comment TODO in `README.md`, executed in the v0.1.0 publish
  PR alongside the actual crates.io upload.
- **`time`/`addr`/`hifi` alias additions.** These modules ship no
  `pub type` aliases today; AGENTS.md's Conn-naming uses 4-letter
  mnemonics (`TDURSECS`, `IPV4`, …) without per-host width aliases.
  Out of scope for this sprint.

## Review

Filled during/after implementation per the gardener rule.
